
var mocha = require('mocha'),
    chai = require('chai'),
    expect = chai.expect;

// feel free to rename this property
var solution = require('../src/<%= kata.slug %>');

describe('<%= kata.name %> (<%= kata.id %>)', function() {
    // implement your tests here
});
